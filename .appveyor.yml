image: Ubuntu  # образ для сборки

stack: jdk 11

branches:
  only:
    - main  # ветка git


build: off

install:

  - chmod +x gradlew
  - java -jar ./artifacts/app-mbank.jar &

build_script:
  - ./gradlew test --info